import React, { Component } from 'react';
import './App.css';

class Header extends Component {
    render() {
        return(
            <div className="head">
                <h2>Zigima Test Add View</h2>
            </div>
        );
    }
}

export default Header;